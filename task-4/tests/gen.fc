
[int, tuple, cell, tuple, int] test_change_22_data() method_id(22) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(12345, 256) ;; key
      .store_uint(100, 64) ;; valid until
      .store_uint(787788, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = begin_cell().end_cell();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_22(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(23) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_24_data() method_id(24) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([12345]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_24(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(25) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 100);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 787788);
}

[int, tuple, cell, tuple, int] test_get_26_data() method_id(26) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([12346]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_26(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(27) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_28_data() method_id(28) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(101), null()];
}


_ test_remove_28(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(29) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_30_data() method_id(30) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([12345]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_30(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(31) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_32_data() method_id(32) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([7]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_32(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(33) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_34_data() method_id(34) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([4]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_34(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(35) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_36_data() method_id(36) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([8]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_36(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(37) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_38_data() method_id(38) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([0]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_38(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(39) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_40_data() method_id(40) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([4]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_40(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(41) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_42_data() method_id(42) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([4]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_42(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(43) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_44_data() method_id(44) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1050), null()];
}


_ test_remove_44(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(45) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_46_data() method_id(46) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1100), null()];
}


_ test_remove_46(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(47) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_48_data() method_id(48) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([4]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_48(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(49) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_50_data() method_id(50) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([7]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_50(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(51) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_52_data() method_id(52) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(5, 256) ;; key
      .store_uint(1169, 64) ;; valid until
      .store_uint(9, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_52(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(53) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_54_data() method_id(54) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([6]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_54(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(55) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_56_data() method_id(56) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(0, 256) ;; key
      .store_uint(1154, 64) ;; valid until
      .store_uint(8, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_56(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(57) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_58_data() method_id(58) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([10]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_58(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(59) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_60_data() method_id(60) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1150), null()];
}


_ test_remove_60(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(61) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_62_data() method_id(62) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(9, 256) ;; key
      .store_uint(1181, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_62(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(63) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_64_data() method_id(64) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(6, 256) ;; key
      .store_uint(1166, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_64(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(65) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_66_data() method_id(66) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([10]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_66(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(67) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_68_data() method_id(68) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([1]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_68(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(69) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_70_data() method_id(70) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(3, 256) ;; key
      .store_uint(1210, 64) ;; valid until
      .store_uint(7, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_70(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(71) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_72_data() method_id(72) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([7]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_72(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(73) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_74_data() method_id(74) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([0]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_74(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(75) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1154);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 8);
}

[int, tuple, cell, tuple, int] test_change_76_data() method_id(76) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(0, 256) ;; key
      .store_uint(1175, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_76(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(77) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_78_data() method_id(78) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([3]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_78(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(79) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1210);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 7);
}

[int, tuple, cell, tuple, int] test_get_80_data() method_id(80) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([7]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_80(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(81) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_82_data() method_id(82) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1200), null()];
}


_ test_remove_82(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(83) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_84_data() method_id(84) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(4, 256) ;; key
      .store_uint(1265, 64) ;; valid until
      .store_uint(3, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_84(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(85) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_86_data() method_id(86) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(8, 256) ;; key
      .store_uint(1231, 64) ;; valid until
      .store_uint(8, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_86(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(87) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_88_data() method_id(88) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(4, 256) ;; key
      .store_uint(1225, 64) ;; valid until
      .store_uint(9, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_88(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(89) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_90_data() method_id(90) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([3]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_90(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(91) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1210);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 7);
}

[int, tuple, cell, tuple, int] test_change_92_data() method_id(92) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(9, 256) ;; key
      .store_uint(1264, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_92(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(93) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_94_data() method_id(94) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(5, 256) ;; key
      .store_uint(1200, 64) ;; valid until
      .store_uint(10, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_94(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(95) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_96_data() method_id(96) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(7, 256) ;; key
      .store_uint(1234, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_96(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(97) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_98_data() method_id(98) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([4]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_98(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(99) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1225);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 9);
}

[int, tuple, cell, tuple, int] test_change_100_data() method_id(100) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(9, 256) ;; key
      .store_uint(1220, 64) ;; valid until
      .store_uint(7, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_100(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(101) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_102_data() method_id(102) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(2, 256) ;; key
      .store_uint(1298, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_102(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(103) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_104_data() method_id(104) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(2, 256) ;; key
      .store_uint(1243, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_104(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(105) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_106_data() method_id(106) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(8, 256) ;; key
      .store_uint(1236, 64) ;; valid until
      .store_uint(3, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_106(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(107) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_108_data() method_id(108) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([2]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_108(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(109) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1243);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 1);
}

[int, tuple, cell, tuple, int] test_change_110_data() method_id(110) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(2, 256) ;; key
      .store_uint(1249, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_110(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(111) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_112_data() method_id(112) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1250), null()];
}


_ test_remove_112(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(113) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_114_data() method_id(114) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([6]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_114(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(115) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_116_data() method_id(116) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(4, 256) ;; key
      .store_uint(1286, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_116(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(117) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_118_data() method_id(118) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(4, 256) ;; key
      .store_uint(1340, 64) ;; valid until
      .store_uint(8, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_118(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(119) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_120_data() method_id(120) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(7, 256) ;; key
      .store_uint(1296, 64) ;; valid until
      .store_uint(4, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_120(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(121) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_122_data() method_id(122) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([8]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_122(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(123) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_124_data() method_id(124) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1300), null()];
}


_ test_remove_124(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(125) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_126_data() method_id(126) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([8]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_126(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(127) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_128_data() method_id(128) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1350), null()];
}


_ test_remove_128(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(129) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_130_data() method_id(130) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(5, 256) ;; key
      .store_uint(1426, 64) ;; valid until
      .store_uint(10, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_130(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(131) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_132_data() method_id(132) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(3, 256) ;; key
      .store_uint(1357, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_132(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(133) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_134_data() method_id(134) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([0]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_134(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(135) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_136_data() method_id(136) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([6]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_136(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(137) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_138_data() method_id(138) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(6, 256) ;; key
      .store_uint(1419, 64) ;; valid until
      .store_uint(3, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_138(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(139) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_140_data() method_id(140) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(9, 256) ;; key
      .store_uint(1393, 64) ;; valid until
      .store_uint(4, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_140(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(141) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_142_data() method_id(142) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([9]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_142(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(143) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1393);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 4);
}

[int, tuple, cell, tuple, int] test_change_144_data() method_id(144) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(1, 256) ;; key
      .store_uint(1393, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_144(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(145) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_146_data() method_id(146) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(4, 256) ;; key
      .store_uint(1401, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_146(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(147) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_148_data() method_id(148) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(8, 256) ;; key
      .store_uint(1397, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_148(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(149) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_150_data() method_id(150) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(0, 256) ;; key
      .store_uint(1425, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_150(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(151) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_152_data() method_id(152) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1400), null()];
}


_ test_remove_152(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(153) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_154_data() method_id(154) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([2]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_154(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(155) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_156_data() method_id(156) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1450), null()];
}


_ test_remove_156(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(157) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_158_data() method_id(158) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(0, 256) ;; key
      .store_uint(1548, 64) ;; valid until
      .store_uint(10, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_158(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(159) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_160_data() method_id(160) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([3]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_160(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(161) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_162_data() method_id(162) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(9, 256) ;; key
      .store_uint(1520, 64) ;; valid until
      .store_uint(4, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_162(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(163) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_164_data() method_id(164) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(7, 256) ;; key
      .store_uint(1534, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_164(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(165) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_166_data() method_id(166) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([6]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_166(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(167) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_168_data() method_id(168) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(4, 256) ;; key
      .store_uint(1478, 64) ;; valid until
      .store_uint(9, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_168(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(169) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_170_data() method_id(170) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([8]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_170(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(171) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_172_data() method_id(172) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(4, 256) ;; key
      .store_uint(1480, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_172(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(173) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_174_data() method_id(174) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([1]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_174(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(175) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_176_data() method_id(176) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1500), null()];
}


_ test_remove_176(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(177) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_178_data() method_id(178) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([8]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_178(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(179) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_180_data() method_id(180) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1550), null()];
}


_ test_remove_180(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(181) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_182_data() method_id(182) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([10]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_182(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(183) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_184_data() method_id(184) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([4]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_184(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(185) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_186_data() method_id(186) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([3]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_186(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(187) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_188_data() method_id(188) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([5]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_188(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(189) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_190_data() method_id(190) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([3]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_190(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(191) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_192_data() method_id(192) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(3, 256) ;; key
      .store_uint(1560, 64) ;; valid until
      .store_uint(7, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_192(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(193) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_194_data() method_id(194) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([8]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_194(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(195) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_196_data() method_id(196) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(8, 256) ;; key
      .store_uint(1622, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_196(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(197) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_198_data() method_id(198) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([7]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_198(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(199) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_200_data() method_id(200) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([4]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_200(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(201) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_202_data() method_id(202) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(4, 256) ;; key
      .store_uint(1618, 64) ;; valid until
      .store_uint(9, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_202(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(203) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_204_data() method_id(204) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(6, 256) ;; key
      .store_uint(1600, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_204(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(205) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_206_data() method_id(206) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([4]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_206(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(207) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1618);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 9);
}

[int, tuple, cell, tuple, int] test_change_208_data() method_id(208) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(3, 256) ;; key
      .store_uint(1617, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_208(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(209) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_210_data() method_id(210) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(5, 256) ;; key
      .store_uint(1568, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_210(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(211) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_212_data() method_id(212) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(4, 256) ;; key
      .store_uint(1588, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_212(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(213) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_214_data() method_id(214) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(0, 256) ;; key
      .store_uint(1557, 64) ;; valid until
      .store_uint(5, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_214(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(215) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_216_data() method_id(216) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([0]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_216(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(217) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1557);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 5);
}

[int, tuple, cell, tuple, int] test_change_218_data() method_id(218) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(4, 256) ;; key
      .store_uint(1606, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_218(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(219) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_220_data() method_id(220) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(3, 256) ;; key
      .store_uint(1622, 64) ;; valid until
      .store_uint(7, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_220(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(221) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_222_data() method_id(222) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([6]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_222(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(223) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1600);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 1);
}

[int, tuple, cell, tuple, int] test_change_224_data() method_id(224) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(1, 256) ;; key
      .store_uint(1553, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_224(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(225) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_226_data() method_id(226) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(10, 256) ;; key
      .store_uint(1635, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_226(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(227) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_228_data() method_id(228) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(1, 256) ;; key
      .store_uint(1586, 64) ;; valid until
      .store_uint(7, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_228(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(229) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_230_data() method_id(230) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(8, 256) ;; key
      .store_uint(1613, 64) ;; valid until
      .store_uint(9, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_230(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(231) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_232_data() method_id(232) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([4]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_232(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(233) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1606);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 1);
}

[int, tuple, cell, tuple, int] test_get_234_data() method_id(234) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([9]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_234(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(235) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_236_data() method_id(236) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([7]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_236(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(237) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_238_data() method_id(238) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(4, 256) ;; key
      .store_uint(1622, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_238(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(239) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_240_data() method_id(240) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(6, 256) ;; key
      .store_uint(1557, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_240(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(241) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_242_data() method_id(242) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([10]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_242(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(243) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1635);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 0);
}

[int, tuple, cell, tuple, int] test_change_244_data() method_id(244) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(1, 256) ;; key
      .store_uint(1580, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_244(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(245) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_246_data() method_id(246) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([9]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_246(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(247) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_248_data() method_id(248) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([7]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_248(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(249) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_250_data() method_id(250) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(6, 256) ;; key
      .store_uint(1645, 64) ;; valid until
      .store_uint(5, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_250(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(251) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_252_data() method_id(252) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(2, 256) ;; key
      .store_uint(1646, 64) ;; valid until
      .store_uint(3, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_252(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(253) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_254_data() method_id(254) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([5]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_254(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(255) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1568);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 6);
}

[int, tuple, cell, tuple, int] test_change_256_data() method_id(256) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(4, 256) ;; key
      .store_uint(1580, 64) ;; valid until
      .store_uint(10, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_256(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(257) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_258_data() method_id(258) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([8]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_258(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(259) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1613);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 9);
}

[int, tuple, cell, tuple, int] test_change_260_data() method_id(260) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(1, 256) ;; key
      .store_uint(1620, 64) ;; valid until
      .store_uint(7, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_260(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(261) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_262_data() method_id(262) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(5, 256) ;; key
      .store_uint(1621, 64) ;; valid until
      .store_uint(5, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_262(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(263) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_264_data() method_id(264) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(7, 256) ;; key
      .store_uint(1616, 64) ;; valid until
      .store_uint(5, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_264(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(265) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_266_data() method_id(266) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([9]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_266(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(267) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_268_data() method_id(268) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(3, 256) ;; key
      .store_uint(1619, 64) ;; valid until
      .store_uint(4, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_268(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(269) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_270_data() method_id(270) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(10, 256) ;; key
      .store_uint(1587, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_270(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(271) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_272_data() method_id(272) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([6]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_272(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(273) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1645);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 5);
}

[int, tuple, cell, tuple, int] test_get_274_data() method_id(274) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([8]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_274(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(275) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1613);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 9);
}

[int, tuple, cell, tuple, int] test_get_276_data() method_id(276) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([2]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_276(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(277) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1646);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 3);
}

[int, tuple, cell, tuple, int] test_remove_278_data() method_id(278) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1600), null()];
}


_ test_remove_278(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(279) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_280_data() method_id(280) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([4]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_280(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(281) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_282_data() method_id(282) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(10, 256) ;; key
      .store_uint(1679, 64) ;; valid until
      .store_uint(4, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_282(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(283) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_284_data() method_id(284) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(1, 256) ;; key
      .store_uint(1659, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_284(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(285) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_286_data() method_id(286) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(0, 256) ;; key
      .store_uint(1669, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_286(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(287) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_288_data() method_id(288) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(3, 256) ;; key
      .store_uint(1681, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_288(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(289) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_290_data() method_id(290) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([5]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_290(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(291) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1621);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 5);
}

[int, tuple, cell, tuple, int] test_get_292_data() method_id(292) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([1]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_292(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(293) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1659);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 0);
}

[int, tuple, cell, tuple, int] test_change_294_data() method_id(294) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(5, 256) ;; key
      .store_uint(1615, 64) ;; valid until
      .store_uint(8, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_294(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(295) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_296_data() method_id(296) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([0]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_296(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(297) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1669);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 1);
}

[int, tuple, cell, tuple, int] test_change_298_data() method_id(298) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(2, 256) ;; key
      .store_uint(1657, 64) ;; valid until
      .store_uint(3, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_298(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(299) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_300_data() method_id(300) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([9]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_300(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(301) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_302_data() method_id(302) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1650), null()];
}


_ test_remove_302(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(303) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_304_data() method_id(304) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([1]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_304(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(305) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1659);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 0);
}

[int, tuple, cell, tuple, int] test_change_306_data() method_id(306) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(10, 256) ;; key
      .store_uint(1716, 64) ;; valid until
      .store_uint(5, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_306(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(307) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_308_data() method_id(308) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([3]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_308(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(309) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1681);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 0);
}

[int, tuple, cell, tuple, int] test_get_310_data() method_id(310) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([9]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_310(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(311) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_312_data() method_id(312) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([0]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_312(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(313) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1669);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 1);
}

[int, tuple, cell, tuple, int] test_change_314_data() method_id(314) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(9, 256) ;; key
      .store_uint(1744, 64) ;; valid until
      .store_uint(9, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_314(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(315) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_316_data() method_id(316) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(0, 256) ;; key
      .store_uint(1684, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_316(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(317) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_318_data() method_id(318) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1700), null()];
}


_ test_remove_318(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(319) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_320_data() method_id(320) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(8, 256) ;; key
      .store_uint(1723, 64) ;; valid until
      .store_uint(9, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_320(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(321) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_322_data() method_id(322) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(8, 256) ;; key
      .store_uint(1765, 64) ;; valid until
      .store_uint(4, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_322(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(323) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_324_data() method_id(324) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([5]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_324(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(325) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_326_data() method_id(326) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(6, 256) ;; key
      .store_uint(1763, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_326(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(327) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_328_data() method_id(328) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(0, 256) ;; key
      .store_uint(1700, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_328(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(329) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_330_data() method_id(330) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([0]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_330(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(331) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1700);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 0);
}

[int, tuple, cell, tuple, int] test_remove_332_data() method_id(332) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1750), null()];
}


_ test_remove_332(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(333) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_334_data() method_id(334) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(7, 256) ;; key
      .store_uint(1807, 64) ;; valid until
      .store_uint(3, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_334(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(335) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_336_data() method_id(336) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(1, 256) ;; key
      .store_uint(1758, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_336(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(337) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_338_data() method_id(338) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([10]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_338(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(339) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_340_data() method_id(340) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([9]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_340(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(341) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_342_data() method_id(342) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([0]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_342(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(343) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_344_data() method_id(344) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(1, 256) ;; key
      .store_uint(1797, 64) ;; valid until
      .store_uint(4, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_344(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(345) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_346_data() method_id(346) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(8, 256) ;; key
      .store_uint(1839, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_346(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(347) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_348_data() method_id(348) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(5, 256) ;; key
      .store_uint(1785, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_348(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(349) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_350_data() method_id(350) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([8]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_350(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(351) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1839);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 2);
}

[int, tuple, cell, tuple, int] test_get_352_data() method_id(352) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([3]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_352(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(353) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_354_data() method_id(354) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(8, 256) ;; key
      .store_uint(1768, 64) ;; valid until
      .store_uint(8, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_354(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(355) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_356_data() method_id(356) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(3, 256) ;; key
      .store_uint(1784, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_356(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(357) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_358_data() method_id(358) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([1]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_358(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(359) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1797);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 4);
}

[int, tuple, cell, tuple, int] test_change_360_data() method_id(360) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(10, 256) ;; key
      .store_uint(1830, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_360(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(361) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_362_data() method_id(362) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(1, 256) ;; key
      .store_uint(1779, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_362(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(363) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_364_data() method_id(364) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([10]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_364(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(365) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1830);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 1);
}

[int, tuple, cell, tuple, int] test_change_366_data() method_id(366) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(2, 256) ;; key
      .store_uint(1796, 64) ;; valid until
      .store_uint(4, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_366(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(367) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_368_data() method_id(368) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([2]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_368(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(369) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1796);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 4);
}

[int, tuple, cell, tuple, int] test_change_370_data() method_id(370) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(7, 256) ;; key
      .store_uint(1837, 64) ;; valid until
      .store_uint(8, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_370(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(371) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_372_data() method_id(372) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(9, 256) ;; key
      .store_uint(1775, 64) ;; valid until
      .store_uint(8, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_372(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(373) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_374_data() method_id(374) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([5]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_374(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(375) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1785);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 2);
}

[int, tuple, cell, tuple, int] test_get_376_data() method_id(376) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([1]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_376(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(377) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1779);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 1);
}

[int, tuple, cell, tuple, int] test_change_378_data() method_id(378) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(1, 256) ;; key
      .store_uint(1802, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_378(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(379) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_380_data() method_id(380) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(6, 256) ;; key
      .store_uint(1799, 64) ;; valid until
      .store_uint(4, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_380(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(381) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_382_data() method_id(382) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(0, 256) ;; key
      .store_uint(1757, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_382(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(383) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_384_data() method_id(384) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([4]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_384(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(385) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_386_data() method_id(386) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(2, 256) ;; key
      .store_uint(1790, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_386(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(387) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_388_data() method_id(388) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(9, 256) ;; key
      .store_uint(1812, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_388(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(389) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_390_data() method_id(390) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([7]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_390(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(391) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1837);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 8);
}

[int, tuple, cell, tuple, int] test_get_392_data() method_id(392) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([5]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_392(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(393) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1785);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 2);
}

[int, tuple, cell, tuple, int] test_get_394_data() method_id(394) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([7]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_394(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(395) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1837);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 8);
}

[int, tuple, cell, tuple, int] test_change_396_data() method_id(396) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(6, 256) ;; key
      .store_uint(1769, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_396(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(397) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_398_data() method_id(398) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(5, 256) ;; key
      .store_uint(1839, 64) ;; valid until
      .store_uint(5, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_398(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(399) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_400_data() method_id(400) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([3]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_400(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(401) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1784);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 1);
}

[int, tuple, cell, tuple, int] test_get_402_data() method_id(402) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([9]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_402(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(403) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1812);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 1);
}

[int, tuple, cell, tuple, int] test_get_404_data() method_id(404) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([2]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_404(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(405) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1790);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 0);
}

[int, tuple, cell, tuple, int] test_get_406_data() method_id(406) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([9]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_406(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(407) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1812);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 1);
}

[int, tuple, cell, tuple, int] test_get_408_data() method_id(408) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([0]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_408(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(409) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1757);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 2);
}

[int, tuple, cell, tuple, int] test_change_410_data() method_id(410) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(6, 256) ;; key
      .store_uint(1777, 64) ;; valid until
      .store_uint(5, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_410(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(411) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_412_data() method_id(412) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([2]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_412(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(413) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1790);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 0);
}

[int, tuple, cell, tuple, int] test_get_414_data() method_id(414) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([1]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_414(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(415) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1802);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 6);
}

[int, tuple, cell, tuple, int] test_remove_416_data() method_id(416) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1800), null()];
}


_ test_remove_416(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(417) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_418_data() method_id(418) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([2]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_418(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(419) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_420_data() method_id(420) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(0, 256) ;; key
      .store_uint(1856, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_420(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(421) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_422_data() method_id(422) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(8, 256) ;; key
      .store_uint(1829, 64) ;; valid until
      .store_uint(7, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_422(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(423) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_424_data() method_id(424) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(1, 256) ;; key
      .store_uint(1858, 64) ;; valid until
      .store_uint(9, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_424(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(425) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_426_data() method_id(426) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(6, 256) ;; key
      .store_uint(1852, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_426(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(427) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_428_data() method_id(428) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([7]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_428(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(429) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1837);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 8);
}

[int, tuple, cell, tuple, int] test_change_430_data() method_id(430) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(0, 256) ;; key
      .store_uint(1877, 64) ;; valid until
      .store_uint(7, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_430(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(431) {
    throw_if(100, exit_code != 0);
}
    