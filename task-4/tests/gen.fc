
[int, tuple, cell, tuple, int] test_change_22_data() method_id(22) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(12345, 256) ;; key
      .store_uint(100, 64) ;; valid until
      .store_uint(787788, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = begin_cell().end_cell();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_22(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(23) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_24_data() method_id(24) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([12345]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_24(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(25) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 100);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 787788);
}

[int, tuple, cell, tuple, int] test_get_26_data() method_id(26) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([12346]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_26(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(27) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_28_data() method_id(28) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(101), null()];
}


_ test_remove_28(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(29) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_30_data() method_id(30) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([12345]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_30(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(31) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_32_data() method_id(32) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(90, 256) ;; key
      .store_uint(1048, 64) ;; valid until
      .store_uint(10, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_32(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(33) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_34_data() method_id(34) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1034), null()];
}


_ test_remove_34(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(35) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_36_data() method_id(36) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1049), null()];
}


_ test_remove_36(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(37) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_38_data() method_id(38) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(314, 256) ;; key
      .store_uint(1005, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_38(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(39) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_40_data() method_id(40) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(643, 256) ;; key
      .store_uint(1025, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_40(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(41) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_42_data() method_id(42) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1083), null()];
}


_ test_remove_42(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(43) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_44_data() method_id(44) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([547]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_44(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(45) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_46_data() method_id(46) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(595, 256) ;; key
      .store_uint(1066, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_46(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(47) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_48_data() method_id(48) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1023), null()];
}


_ test_remove_48(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(49) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_50_data() method_id(50) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(595, 256) ;; key
      .store_uint(1079, 64) ;; valid until
      .store_uint(5, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_50(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(51) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_52_data() method_id(52) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1066), null()];
}


_ test_remove_52(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(53) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_54_data() method_id(54) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([385]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_54(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(55) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_56_data() method_id(56) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(607, 256) ;; key
      .store_uint(1090, 64) ;; valid until
      .store_uint(5, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_56(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(57) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_58_data() method_id(58) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1042), null()];
}


_ test_remove_58(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(59) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_60_data() method_id(60) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(770, 256) ;; key
      .store_uint(1062, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_60(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(61) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_62_data() method_id(62) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(232, 256) ;; key
      .store_uint(1046, 64) ;; valid until
      .store_uint(7, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_62(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(63) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_64_data() method_id(64) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(699, 256) ;; key
      .store_uint(1037, 64) ;; valid until
      .store_uint(3, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_64(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(65) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_66_data() method_id(66) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([949]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_66(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(67) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_68_data() method_id(68) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([407]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_68(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(69) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_70_data() method_id(70) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1002), null()];
}


_ test_remove_70(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(71) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_72_data() method_id(72) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(566, 256) ;; key
      .store_uint(1002, 64) ;; valid until
      .store_uint(4, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_72(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(73) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_74_data() method_id(74) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1038), null()];
}


_ test_remove_74(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(75) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_76_data() method_id(76) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1004), null()];
}


_ test_remove_76(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(77) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_78_data() method_id(78) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1067), null()];
}


_ test_remove_78(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(79) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_80_data() method_id(80) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([693]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_80(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(81) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_82_data() method_id(82) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(327, 256) ;; key
      .store_uint(1002, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_82(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(83) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_84_data() method_id(84) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([887]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_84(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(85) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_86_data() method_id(86) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1028), null()];
}


_ test_remove_86(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(87) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_88_data() method_id(88) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1061), null()];
}


_ test_remove_88(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(89) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_90_data() method_id(90) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1054), null()];
}


_ test_remove_90(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(91) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_92_data() method_id(92) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1079), null()];
}


_ test_remove_92(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(93) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_94_data() method_id(94) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1069), null()];
}


_ test_remove_94(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(95) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_96_data() method_id(96) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1081), null()];
}


_ test_remove_96(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(97) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_98_data() method_id(98) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(529, 256) ;; key
      .store_uint(1075, 64) ;; valid until
      .store_uint(10, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_98(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(99) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_100_data() method_id(100) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([501]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_100(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(101) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_102_data() method_id(102) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(737, 256) ;; key
      .store_uint(1018, 64) ;; valid until
      .store_uint(4, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_102(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(103) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_104_data() method_id(104) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(531, 256) ;; key
      .store_uint(1023, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_104(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(105) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_106_data() method_id(106) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([919]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_106(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(107) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_108_data() method_id(108) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(699, 256) ;; key
      .store_uint(1082, 64) ;; valid until
      .store_uint(10, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_108(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(109) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_110_data() method_id(110) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([236]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_110(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(111) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_112_data() method_id(112) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([626]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_112(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(113) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_114_data() method_id(114) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1054), null()];
}


_ test_remove_114(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(115) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_116_data() method_id(116) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(646, 256) ;; key
      .store_uint(1029, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_116(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(117) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_118_data() method_id(118) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(378, 256) ;; key
      .store_uint(1027, 64) ;; valid until
      .store_uint(7, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_118(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(119) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_120_data() method_id(120) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1050), null()];
}


_ test_remove_120(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(121) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_122_data() method_id(122) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([611]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_122(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(123) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_124_data() method_id(124) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(108, 256) ;; key
      .store_uint(1098, 64) ;; valid until
      .store_uint(9, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_124(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(125) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_126_data() method_id(126) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([16]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_126(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(127) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_128_data() method_id(128) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1024), null()];
}


_ test_remove_128(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(129) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_130_data() method_id(130) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1079), null()];
}


_ test_remove_130(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(131) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_132_data() method_id(132) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([588]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_132(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(133) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_134_data() method_id(134) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(600, 256) ;; key
      .store_uint(1023, 64) ;; valid until
      .store_uint(9, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_134(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(135) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_136_data() method_id(136) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(22, 256) ;; key
      .store_uint(1046, 64) ;; valid until
      .store_uint(7, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_136(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(137) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_138_data() method_id(138) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(226, 256) ;; key
      .store_uint(1035, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_138(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(139) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_140_data() method_id(140) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(498, 256) ;; key
      .store_uint(1088, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_140(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(141) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_142_data() method_id(142) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(578, 256) ;; key
      .store_uint(1086, 64) ;; valid until
      .store_uint(7, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_142(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(143) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_144_data() method_id(144) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([699]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_144(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(145) {
    throw_if(100, exit_code != 0);

    var valid_until = first(stack);
    throw_if(101, valid_until != 1082);
    var value = second(stack);
    throw_if(102, value~load_uint(128) != 10);
}

[int, tuple, cell, tuple, int] test_get_146_data() method_id(146) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([33]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_146(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(147) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_148_data() method_id(148) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([540]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_148(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(149) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_150_data() method_id(150) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1080), null()];
}


_ test_remove_150(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(151) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_152_data() method_id(152) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1060), null()];
}


_ test_remove_152(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(153) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_154_data() method_id(154) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([752]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_154(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(155) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_156_data() method_id(156) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([371]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_156(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(157) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_158_data() method_id(158) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(672, 256) ;; key
      .store_uint(1083, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_158(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(159) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_160_data() method_id(160) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([151]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_160(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(161) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_162_data() method_id(162) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1090), null()];
}


_ test_remove_162(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(163) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_164_data() method_id(164) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(109, 256) ;; key
      .store_uint(1002, 64) ;; valid until
      .store_uint(8, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_164(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(165) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_166_data() method_id(166) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([794]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_166(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(167) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_168_data() method_id(168) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([122]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_168(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(169) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_170_data() method_id(170) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(562, 256) ;; key
      .store_uint(1059, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_170(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(171) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_172_data() method_id(172) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(816, 256) ;; key
      .store_uint(1070, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_172(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(173) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_174_data() method_id(174) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([952]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_174(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(175) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_176_data() method_id(176) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([628]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_176(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(177) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_178_data() method_id(178) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(254, 256) ;; key
      .store_uint(1073, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_178(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(179) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_180_data() method_id(180) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([372]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_180(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(181) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_182_data() method_id(182) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(414, 256) ;; key
      .store_uint(1009, 64) ;; valid until
      .store_uint(7, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_182(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(183) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_184_data() method_id(184) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1035), null()];
}


_ test_remove_184(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(185) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_186_data() method_id(186) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(127, 256) ;; key
      .store_uint(1079, 64) ;; valid until
      .store_uint(10, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_186(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(187) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_188_data() method_id(188) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([63]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_188(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(189) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_190_data() method_id(190) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1017), null()];
}


_ test_remove_190(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(191) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_192_data() method_id(192) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([684]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_192(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(193) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_194_data() method_id(194) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1091), null()];
}


_ test_remove_194(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(195) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_196_data() method_id(196) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(406, 256) ;; key
      .store_uint(1029, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_196(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(197) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_198_data() method_id(198) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([810]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_198(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(199) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_200_data() method_id(200) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(303, 256) ;; key
      .store_uint(1096, 64) ;; valid until
      .store_uint(10, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_200(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(201) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_202_data() method_id(202) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([422]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_202(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(203) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_204_data() method_id(204) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1021), null()];
}


_ test_remove_204(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(205) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_206_data() method_id(206) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1026), null()];
}


_ test_remove_206(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(207) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_208_data() method_id(208) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([153]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_208(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(209) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_210_data() method_id(210) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([916]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_210(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(211) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_212_data() method_id(212) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(191, 256) ;; key
      .store_uint(1009, 64) ;; valid until
      .store_uint(7, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_212(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(213) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_214_data() method_id(214) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([557]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_214(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(215) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_216_data() method_id(216) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([290]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_216(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(217) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_218_data() method_id(218) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(75, 256) ;; key
      .store_uint(1038, 64) ;; valid until
      .store_uint(5, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_218(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(219) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_220_data() method_id(220) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(200, 256) ;; key
      .store_uint(1053, 64) ;; valid until
      .store_uint(4, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_220(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(221) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_222_data() method_id(222) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1009), null()];
}


_ test_remove_222(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(223) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_224_data() method_id(224) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(350, 256) ;; key
      .store_uint(1037, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_224(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(225) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_226_data() method_id(226) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([958]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_226(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(227) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_228_data() method_id(228) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1065), null()];
}


_ test_remove_228(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(229) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_230_data() method_id(230) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(447, 256) ;; key
      .store_uint(1067, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_230(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(231) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_232_data() method_id(232) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(232, 256) ;; key
      .store_uint(1033, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_232(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(233) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_234_data() method_id(234) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(436, 256) ;; key
      .store_uint(1024, 64) ;; valid until
      .store_uint(3, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_234(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(235) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_236_data() method_id(236) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(509, 256) ;; key
      .store_uint(1076, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_236(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(237) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_238_data() method_id(238) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([870]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_238(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(239) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_240_data() method_id(240) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([95]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_240(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(241) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_242_data() method_id(242) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(130, 256) ;; key
      .store_uint(1009, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_242(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(243) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_244_data() method_id(244) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([751]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_244(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(245) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_246_data() method_id(246) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(101, 256) ;; key
      .store_uint(1060, 64) ;; valid until
      .store_uint(7, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_246(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(247) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_248_data() method_id(248) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(902, 256) ;; key
      .store_uint(1006, 64) ;; valid until
      .store_uint(10, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_248(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(249) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_250_data() method_id(250) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(549, 256) ;; key
      .store_uint(1039, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_250(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(251) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_252_data() method_id(252) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(888, 256) ;; key
      .store_uint(1040, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_252(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(253) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_254_data() method_id(254) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(314, 256) ;; key
      .store_uint(1011, 64) ;; valid until
      .store_uint(10, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_254(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(255) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_256_data() method_id(256) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1072), null()];
}


_ test_remove_256(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(257) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_258_data() method_id(258) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([845]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_258(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(259) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_260_data() method_id(260) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([123]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_260(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(261) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_262_data() method_id(262) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([145]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_262(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(263) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_264_data() method_id(264) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1097), null()];
}


_ test_remove_264(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(265) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_266_data() method_id(266) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1000), null()];
}


_ test_remove_266(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(267) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_268_data() method_id(268) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(957, 256) ;; key
      .store_uint(1067, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_268(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(269) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_270_data() method_id(270) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(56, 256) ;; key
      .store_uint(1004, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_270(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(271) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_272_data() method_id(272) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(882, 256) ;; key
      .store_uint(1043, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_272(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(273) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_274_data() method_id(274) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1060), null()];
}


_ test_remove_274(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(275) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_276_data() method_id(276) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(697, 256) ;; key
      .store_uint(1044, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_276(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(277) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_278_data() method_id(278) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1024), null()];
}


_ test_remove_278(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(279) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_280_data() method_id(280) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([239]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_280(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(281) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_282_data() method_id(282) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1000), null()];
}


_ test_remove_282(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(283) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_284_data() method_id(284) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(375, 256) ;; key
      .store_uint(1041, 64) ;; valid until
      .store_uint(4, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_284(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(285) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_286_data() method_id(286) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(350, 256) ;; key
      .store_uint(1056, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_286(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(287) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_288_data() method_id(288) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([445]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_288(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(289) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_290_data() method_id(290) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(173, 256) ;; key
      .store_uint(1053, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_290(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(291) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_292_data() method_id(292) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1028), null()];
}


_ test_remove_292(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(293) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_294_data() method_id(294) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1091), null()];
}


_ test_remove_294(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(295) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_296_data() method_id(296) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1054), null()];
}


_ test_remove_296(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(297) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_298_data() method_id(298) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([277]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_298(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(299) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_300_data() method_id(300) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(733, 256) ;; key
      .store_uint(1057, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_300(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(301) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_302_data() method_id(302) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(976, 256) ;; key
      .store_uint(1066, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_302(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(303) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_304_data() method_id(304) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([466]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_304(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(305) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_306_data() method_id(306) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(311, 256) ;; key
      .store_uint(1044, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_306(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(307) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_308_data() method_id(308) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1082), null()];
}


_ test_remove_308(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(309) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_310_data() method_id(310) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(16, 256) ;; key
      .store_uint(1035, 64) ;; valid until
      .store_uint(8, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_310(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(311) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_312_data() method_id(312) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1014), null()];
}


_ test_remove_312(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(313) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_314_data() method_id(314) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([796]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_314(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(315) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_316_data() method_id(316) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(266, 256) ;; key
      .store_uint(1032, 64) ;; valid until
      .store_uint(3, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_316(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(317) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_318_data() method_id(318) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(751, 256) ;; key
      .store_uint(1098, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_318(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(319) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_320_data() method_id(320) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(166, 256) ;; key
      .store_uint(1045, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_320(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(321) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_322_data() method_id(322) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1077), null()];
}


_ test_remove_322(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(323) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_324_data() method_id(324) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([149]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_324(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(325) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_326_data() method_id(326) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([480]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_326(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(327) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_328_data() method_id(328) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1068), null()];
}


_ test_remove_328(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(329) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_330_data() method_id(330) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([490]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_330(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(331) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_332_data() method_id(332) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(654, 256) ;; key
      .store_uint(1070, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_332(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(333) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_334_data() method_id(334) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(342, 256) ;; key
      .store_uint(1001, 64) ;; valid until
      .store_uint(7, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_334(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(335) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_336_data() method_id(336) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1017), null()];
}


_ test_remove_336(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(337) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_338_data() method_id(338) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1004), null()];
}


_ test_remove_338(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(339) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_340_data() method_id(340) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1001), null()];
}


_ test_remove_340(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(341) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_342_data() method_id(342) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(439, 256) ;; key
      .store_uint(1011, 64) ;; valid until
      .store_uint(2, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_342(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(343) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_344_data() method_id(344) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(699, 256) ;; key
      .store_uint(1002, 64) ;; valid until
      .store_uint(6, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_344(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(345) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_346_data() method_id(346) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([692]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_346(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(347) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_348_data() method_id(348) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(174, 256) ;; key
      .store_uint(1076, 64) ;; valid until
      .store_uint(3, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_348(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(349) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_350_data() method_id(350) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1080), null()];
}


_ test_remove_350(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(351) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_352_data() method_id(352) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(79, 256) ;; key
      .store_uint(1063, 64) ;; valid until
      .store_uint(9, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_352(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(353) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_354_data() method_id(354) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(294, 256) ;; key
      .store_uint(1022, 64) ;; valid until
      .store_uint(4, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_354(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(355) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_356_data() method_id(356) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([64]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_356(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(357) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_358_data() method_id(358) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(412, 256) ;; key
      .store_uint(1052, 64) ;; valid until
      .store_uint(1, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_358(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(359) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_360_data() method_id(360) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([610]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_360(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(361) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_362_data() method_id(362) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([841]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_362(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(363) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_364_data() method_id(364) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(257, 256) ;; key
      .store_uint(1052, 64) ;; valid until
      .store_uint(3, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_364(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(365) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_366_data() method_id(366) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1068), null()];
}


_ test_remove_366(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(367) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_368_data() method_id(368) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1000), null()];
}


_ test_remove_368(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(369) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_370_data() method_id(370) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([605]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_370(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(371) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_372_data() method_id(372) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1009), null()];
}


_ test_remove_372(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(373) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_374_data() method_id(374) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(43, 256) ;; key
      .store_uint(1047, 64) ;; valid until
      .store_uint(5, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_374(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(375) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_376_data() method_id(376) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([192]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_376(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(377) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_378_data() method_id(378) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([934]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_378(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(379) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_380_data() method_id(380) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1069), null()];
}


_ test_remove_380(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(381) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_382_data() method_id(382) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([897]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_382(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(383) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_384_data() method_id(384) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([890]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_384(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(385) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_386_data() method_id(386) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([441]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_386(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(387) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_388_data() method_id(388) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1058), null()];
}


_ test_remove_388(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(389) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_390_data() method_id(390) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1021), null()];
}


_ test_remove_390(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(391) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_392_data() method_id(392) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(959, 256) ;; key
      .store_uint(1097, 64) ;; valid until
      .store_uint(4, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_392(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(393) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_394_data() method_id(394) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([806]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_394(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(395) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_396_data() method_id(396) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1060), null()];
}


_ test_remove_396(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(397) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_remove_398_data() method_id(398) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1026), null()];
}


_ test_remove_398(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(399) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_400_data() method_id(400) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([922]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_400(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(401) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_402_data() method_id(402) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(682, 256) ;; key
      .store_uint(1088, 64) ;; valid until
      .store_uint(0, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_402(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(403) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_404_data() method_id(404) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(444, 256) ;; key
      .store_uint(1012, 64) ;; valid until
      .store_uint(9, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_404(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(405) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_406_data() method_id(406) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(152, 256) ;; key
      .store_uint(1011, 64) ;; valid until
      .store_uint(8, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_406(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(407) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_408_data() method_id(408) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(225, 256) ;; key
      .store_uint(1060, 64) ;; valid until
      .store_uint(5, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_408(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(409) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_410_data() method_id(410) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([586]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_410(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(411) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_412_data() method_id(412) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(422, 256) ;; key
      .store_uint(1053, 64) ;; valid until
      .store_uint(4, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_412(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(413) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_change_414_data() method_id(414) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(166, 256) ;; key
      .store_uint(1074, 64) ;; valid until
      .store_uint(4, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_414(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(415) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_416_data() method_id(416) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([616]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_416(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(417) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_remove_418_data() method_id(418) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(2, 32) ;; remove old
      .store_uint(12345, 64) ;; query id
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7_now(1028), null()];
}


_ test_remove_418(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(419) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_420_data() method_id(420) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([840]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_420(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(421) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_get_422_data() method_id(422) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([690]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_422(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(423) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_424_data() method_id(424) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(551, 256) ;; key
      .store_uint(1061, 64) ;; valid until
      .store_uint(8, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_424(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(425) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_426_data() method_id(426) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([342]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_426(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(427) {
    throw_if(100, exit_code == 0);
}

[int, tuple, cell, tuple, int] test_change_428_data() method_id(428) {
    int function_selector = 0;

    slice message_body = begin_cell()
      .store_uint(1, 32) ;; add key
      .store_uint(12345, 64) ;; query id
      .store_uint(291, 256) ;; key
      .store_uint(1042, 64) ;; valid until
      .store_uint(5, 128) ;; 128-bit value
      .end_cell().begin_parse();

    cell message = begin_cell()
            .store_uint(0x18, 6)
            .store_uint(0, 2) ;; should be contract address
            .store_grams(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_slice(message_body)
            .end_cell();

    ;; int balance, int msg_value, cell in_msg_full, slice in_msg_body
    tuple stack = unsafe_tuple([12345, 100, message, message_body]);

    cell data = get_prev_c4();

    return [function_selector, stack, data, get_c7(), null()];
}


_ test_change_428(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(429) {
    throw_if(100, exit_code != 0);
}
    
[int, tuple, cell, tuple, int] test_get_430_data() method_id(430) {
    int function_selector = 127977;

    tuple stack = unsafe_tuple([437]);
    cell data = begin_cell().end_cell();

    return [function_selector, stack, get_prev_c4(), get_c7(), null()];
}


_ test_get_430(int exit_code, cell data, tuple stack, cell actions, int gas) method_id(431) {
    throw_if(100, exit_code == 0);
}
